@{
  ViewData["Title"] = "Akses Ditolak";
}
<div class="alert alert-danger mt-4">Anda tidak memiliki akses ke halaman ini.</div>
<a href="@Url.Action("Index", "Home")" class="btn btn-primary">Kembali ke Beranda</a>
